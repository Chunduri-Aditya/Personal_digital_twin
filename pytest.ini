[pytest]
# Collect only tests/. scripts/dev/finish/backup_pre_c/ holds the pre-restyle copy of tests/ (docs/PLAN_FINISH.md P2,
# the rollback for the UI restyle); collecting it too raises ImportPathMismatchError for tests.conftest.
testpaths = tests
